{
  "name": "@elizaos/example-code",
  "version": "2.0.0-beta.1",
  "description": "Async coding agent terminal app - Claude Code meets ElizaOS",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/elizaOS/eliza.git",
    "directory": "packages/examples/code"
  },
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "eliza-code": "./dist/index.js",
    "eliza-code-acp": "./dist/acp.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "bun --conditions eliza-source src/index.ts",
    "dev": "bun --conditions eliza-source --watch src/index.ts",
    "build": "bun build --conditions eliza-source src/index.ts --outdir dist --target bun --external @elizaos/core --external \"@elizaos/plugin-*\" && bun build --conditions eliza-source src/acp.ts --outdir dist --target bun --external @elizaos/core --external \"@elizaos/plugin-*\" && node scripts/write-dist-tsconfig.mjs",
    "test": "bun test --conditions=eliza-source src",
    "e2e:subagents": "bun src/e2e/subagents.ts",
    "e2e:games": "bun src/e2e/game-generation.ts",
    "e2e:deterministic-replay": "bun --conditions eliza-source --tsconfig-override ../../../tsconfig.json tests/e2e/deterministic-app-build-replay.mjs",
    "e2e:owned-runtime-live": "bun run --cwd ../../logger build && bun run --cwd ../../core build:node && bun run --cwd ../../../plugins/plugin-sql build && bun run --cwd ../../../plugins/plugin-coding-tools build && bun run --cwd ../../../plugins/plugin-openai build && bun run build && bun --conditions eliza-source --tsconfig-override ../../../tsconfig.json tests/e2e/owned-runtime-live.mjs",
    "lint": "bunx @biomejs/biome check --write --unsafe .",
    "lint:check": "bunx @biomejs/biome check .",
    "format": "bunx @biomejs/biome format --write .",
    "format:check": "bunx @biomejs/biome format .",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.0.0",
    "@elizaos/core": "workspace:*",
    "@elizaos/plugin-agent-orchestrator": "workspace:*",
    "@elizaos/plugin-anthropic": "workspace:*",
    "@elizaos/plugin-coding-tools": "workspace:*",
    "@elizaos/plugin-goals": "workspace:*",
    "@elizaos/plugin-mcp": "workspace:*",
    "@elizaos/plugin-openai": "workspace:*",
    "@elizaos/plugin-sql": "workspace:*",
    "@elizaos/tui": "latest",
    "chalk": "^5.3.0",
    "dotenv": "^17.2.3",
    "uuid": "^14.0.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@xterm/headless": "^6.0.0",
    "@types/node": "^25.0.3",
    "@typescript/native": "npm:typescript@^7.0.2",
    "typescript": "^6.0.3"
  },
  "elizaos": {
    "scripts": {
      "testLanes": [
        "server"
      ]
    }
  }
}
